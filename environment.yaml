# environment.yaml
name: speech-to-text-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - flask
      - openai
      - requests
      - pypdf
      - python-docx
      - pandas
      - openpyxl
